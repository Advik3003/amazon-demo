# User Service - PRODUCTION environment overrides
spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/user_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    hikari:
      maximum-pool-size: 15
  jpa:
    hibernate:
      ddl-auto: validate
