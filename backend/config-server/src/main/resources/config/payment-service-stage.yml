# Payment Service - STAGING environment overrides
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/payment_db
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
