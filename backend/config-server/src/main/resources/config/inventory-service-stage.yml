# Inventory Service - STAGING environment overrides
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/inventory_db
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
